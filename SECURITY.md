# Security Policy

Math Hero is a client-side static game with no backend, no authentication, and no user data, so the attack surface is minimal.

## Reporting a vulnerability

Please open a private security advisory via the GitHub Security tab (Report a vulnerability), or email bheng.code@gmail.com. Dependency advisories are watched via `npm audit` in CI.
